
// Types for the vehicles page

export interface ExtendedCarInfo {
    id: string;
    model: string;
    brand: string;
    licensePlate: string;
    color: string;
    image: string;
    images: string[];
    available: boolean;
    transmission: string;
}
